// logical operator &&  and 
// logical or operator || 
// logical not operator !


console.log((10>20) && (20>5)); // Evaluates to true only when both values evaluate to true
console.log((10>20) || (20>5)); // Evaluates to true only when one eithere vaules evaluates to true
console.log(!((10>20) || (20>5)));// The not operator will reverse the logic
 
